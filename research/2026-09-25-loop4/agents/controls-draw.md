Controls for loop 4, drawn 2026-09-25.

Pool: YC Fall 2026 companies with industry "B2B" in the yc-oss public batch file (https://yc-oss.github.io/api/batches/fall-2026.json, 91 companies, 45 B2B), minus the 15 B2B companies already in the control bank. 30 remained. Drawn with `random.Random(20260927).sample(pool_sorted_by_name, 3)`.

1. herdr (open-source runtime for fleets of coding agents)
2. Hopper (voice inference: speech models post-trained on a customer's own calls)
3. ORO AI (open, daily-changing benchmark for AI shopping agents, run as a Bittensor subnet)

Sources: YC batch file descriptions and each company's website (herdr.dev, withhopper.com, oroagents.com and its docs), fetched 2026-09-25. Hopper publishes a per-token price. herdr and ORO do not publish pricing, so their last sentence gives the usual model for the category (marked in paragraphs.md).

Note: herdr's space is close to loop 3's Agent Relay control. Kept as drawn; the draw rule excludes banked companies, not banked categories.
