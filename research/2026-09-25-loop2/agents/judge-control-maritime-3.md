# Judge, P5 (control: Maritime), judge 3 of 3 — loop 2, 2026-09-25

Saved by the orchestrator from the judge's final reply. Scores: need 3, value 2, market 4, risk 2 (total 11). VERDICT: PASS.

Treated Maritime (maritime.sh, YC F26) as this team.

**Claims.** Price confirmed: Starter $20 for 20, Growth $100 for 100, Scale $500 for 500; extras $1.50/$1.25/$1.00; 1 vCPU, 2 GB, 5 GB SSD; sleeping machines free; no compute metering but concurrency capped (Scale: 60 of 500 awake) ([pricing](https://maritime.sh/pricing)). Firecracker microVMs snapshotted to disk; wake stated as 0.6 s, 674 ms median, "~1 s" ([home](https://maritime.sh/), [blog](https://maritime.sh/blog/the-real-cost-of-running-ai-agents)). "$1 vs $30–80" true only against always-on machines; idle figures are Maritime's own. Logos unverified; 3 people; no traction figures ([YC](https://www.ycombinator.com/companies/maritime)).

**Who else.** Fly.io "Computers for agents" — Sprites persistent VMs, no idle compute, cold storage $0.02/GB-month ([Sprites](https://fly.io/sprites/)). Cloudflare Sandboxes GA, sleep/wake, active-CPU billing, Figma Make ([blog](https://blog.cloudflare.com/sandbox-ga/)). Blaxel indefinite standby, ~25 ms resume ([pricing](https://blaxel.ai/pricing)). E2B indefinite pause ([docs](https://e2b.dev/docs/sandbox/persistence)). AWS AgentCore microVM per session ([AWS](https://aws.amazon.com/about-aws/whats-new/2026/03/bedrock-agentcore-runtime-session-storage)). Funding: Modal $355M at $4.65B; Daytona $24M plus a filed $48M; E2B $21M, >1B sandboxes ([bex.co](https://bex.co/blog/2026/09/11/ai-sandbox-funding-modal-daytona-e2b), [E2B](https://e2b.dev/blog/series-a)). Data not owned; channel largely owned by AWS, Cloudflare, Vercel, Fly; frameworks route to built-in sandbox providers.

**1. Strongest version.** Fleet management for per-user agents, sold to SaaS companies giving each user a persistent agent (hosted OpenClaw, "AI employee", vertical assistants): machine per end-user with logged-in sessions; wake on email/Slack/SMS/webhook/cron; image rollout and state migration across 10,000 machines; per-tenant cost and hard caps; in-customer-AWS deployment. Flat per-end-user price fits their per-seat economics. Leave coding and RL sandboxes to E2B and Modal.

**2. Ratings.** Need 3 (Figma Make on Cloudflare; hosted OpenClaw at ~$16–39/instance ([Blink](https://blink.new/blog/openclaw-managed-hosting-comparison-2026)); many teams fine with stateless agent plus database). Value 2 (at Maritime's profile, ~11 min awake/day, the judge's arithmetic gives ~$0.97/mo on Sprites and ~$0.86 on Blaxel; only flat pricing differs). Market 4 (Modal $4.65B, Daytona $48M, E2B; but $1/agent needs millions). Risk 2 (five funded players or hyperscalers ship the primitive; flat pricing exposed to heavy users).

**3. Kill and test.** Kill: sleep/wake persistent VMs are a commodity and fleet management plus flat pricing don't justify moving. Test: in two weeks, 10 teams running ≥500 per-user agents; get infra cost per agent and engineering hours; with Sprites and Blaxel prices on the table, ask for a paid fleet migration at $1–2/agent. Fewer than 3 signing means a feature, not a company.
